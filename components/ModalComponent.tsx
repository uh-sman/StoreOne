import React from 'react'
import { ModalComponentProps } from "@/lib/types";
const ModalComponent: React.FC<ModalComponentProps> = ({ onClose, modalType }) => {
  return (
    <div>
      
    </div>
  )
}

export default ModalComponent
